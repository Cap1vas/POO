package Provas.P12022;

public class Q1 {
    /*A diferenca de sobrecarga e sobreescrita é a seguinte:
    Sobrecarga é quando há mais de um método (na mesma classe) com o mesmo nome e que recebem parâmetros distintos.
    A sobreescrita significa que em duas classes por exemplo: Classe pai é A e classe filha é B, sendo classe B uma subclasse da classe A,portanto,
    caso exista um método com a mesma assinatura nessas duas classes, ocorre um evento em tempo de execucao que faz com que ele execute o primeiro encontro com essa classe, ou seja,
    durante o tempo de execucao ocorre uma varredura, a partir da classe mais específica (aquela na qual o variavel é do mesmo tipo em do objeto que foi instanciado nela) e vai subindo na hierarquia.
    O método a ser utilizado é aquele que será encontrado primeiro nessa busca.
     * 
     * 
     */
}
